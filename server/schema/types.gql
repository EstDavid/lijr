module.exports = `
  type User {
    _id: ID!
    createdAt: String!
    updatedAt: String!
    email: String!
    password: String!
    name: String!
    birthDate: String!
    Entries: Int
    LifeAspects: [ID]
  }

  type Query {
    getUserById(userId: ID!): User
    userCount: Int!
  }
`
